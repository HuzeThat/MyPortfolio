# TODO - Add “My Certificate” section

- [ ] Update `portfolio.html`
  - Add nav link to `#certificate`
  - Add new `<section id="certificate">` with certificate cards
  - Use certificate image(s) once provided; for now use placeholder layout
- [ ] Update `styles.css`
  - Add styling rules for `#certificate` cards and certificate image sizing
- [ ] Test
  - Open `portfolio.html` in browser and verify scrolling + layout
